{
  "name": "jobarms-apply-arm",
  "version": "0.1.0",
  "description": "JobArms apply arm - Browser Rendering + Workflows worker that fills and submits job applications",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudflare/playwright": "^1.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.0",
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.1",
    "wrangler": "^4.113.0"
  },
  "overrides": {
    "sharp": "^0.35.3"
  }
}
